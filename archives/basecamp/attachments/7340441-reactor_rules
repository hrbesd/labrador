# #代表注释
# 每一块以一个元素名称或者元素名称下面对应的某个属性开头（元素名称与属性之间使用空格分割）
# -> 表示条件（如果没有，则表示所有标签），每一块可以有多个->条件，条件中的+表示包含，-表示缺失
# => 表示执行的动作，动作包括：
#		remove（删除这个标签）
#		fill(configFilePath)（读取配置文件，填充内容，参数是配置文件名称）
#		replace(target)（用target标签替换原来的标签）
#		set(value)（设置某个属性的值）
# ==> 输出日志，由两部分组成：level | message，分别对应日志等级和日志记录的消息
#     日志的level包括：info,warning,error,debug
#
# 说明：由于有些标签要判断内容是否为空，这个无法表示，所以定义了一个关键字contents，表示标签的内容

applet
	=> remove
	==> warning | 盲人不可使用applet标签，请替换

# img标签的alt属性
img alt
	=> fill(img_alt.config)

video track
	=> fill(video_alt.config)

audio track
	=> fill(audio_alt.config)

img
	-> - alt
	=> remove

area
	=> remove

map
	=> remove

iframe
    => remove

script
	=> remove

style
	=> remove

object
	=> remove

audio
	-> track
	=> remove

video
	-> track
	=> remove

embed
	-> type=audio
	-> type=video
	=> remove

form
	-> + action
	=> remove

canvas
	-> - contents
	=> remove

table
	-> - summary
	==> warning | 表格缺少描述信息

textarea
	-> rows <= 3
	-> cols <= 20
	==> warning | 文本区域太小

figure
	==> warning | 图片可能无法被识别

bb
	==> warning | 我也不知道这是什么标签

a target
	-> _blank
	=> replace(_self)

a
	-> - title
	-> insideSite(src)
	=> set(pageTitle(src))

a
	-> - title
	-> outsideSite(src)
	=> set(点击此连接将转出本网站)
