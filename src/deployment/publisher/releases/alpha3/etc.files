# Use symbolic links to specify which file or directory to copy
# Note: Avoid using relative path when creating links

# Config and templets
ln -s $DEV_SERVER_REPO_ROOT/src/deployment/etc/labrador.conf $TMP_DIR/etc/labrador.conf
ln -s $DEV_SERVER_REPO_ROOT/src/deployment/etc/templets $TMP_DIR/etc/templets
