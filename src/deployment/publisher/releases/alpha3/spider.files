# Use symbolic links to specify which file or directory to copy
# Note: Avoid using relative path when creating links

# Debug
# Actual path may be: ~/labrador_bin/spider

ln -s $DEV_SERVER_REPO_ROOT/src/deployment/debug/spider $TMP_DIR/butts/spider/spider
ln -s $DEV_SERVER_REPO_ROOT/src/deployment/debug/generator $TMP_DIR/butts/spider/generator
